# 모바일 반응형 디자인 개선 기획안

> 작성일: 2026-01-20
> 버전: v1.0

## 1. 개요

### 1.1 배경
모바일 기기에서 P.O.MFS 관리 시스템 접속 시 다음과 같은 UI 문제가 발생:
- 헤더 버튼들이 제목과 겹치거나 화면 밖으로 밀려남
- 테이블 내용이 잘려서 보이지 않음
- 통계 카드가 너무 좁게 표시됨

### 1.2 목표
- 모바일(~768px) 환경에서 완전한 사용성 확보
- 터치 친화적인 UI 요소 크기 적용
- 기존 데스크톱 레이아웃 유지하면서 반응형 지원

---

## 2. 현재 상태 분석

### 2.1 CSS 구조
| 항목 | 현재 상태 | 문제점 |
|------|----------|--------|
| 미디어 쿼리 | ❌ 없음 | 화면 크기에 관계없이 동일한 레이아웃 |
| 헤더 구조 | `display: flex; justify-content: space-between` | 모바일에서 제목+버튼이 한 줄에 강제 배치 |
| 버튼 그룹 | `<nav>` 안에 버튼들이 연속 배치 | 줄바꿈 없이 가로로 밀려남 |
| 테이블 | 반응형 처리 없음 | 컬럼이 많아 모바일에서 내용 잘림 |
| Bootstrap | ✅ v5.3 로드됨 | 활용 가능하나 현재 미사용 |

### 2.2 문제 스크린샷
- `attached_assets/IMG_3784_1768890801948.png`: 메인 페이지 헤더 버튼 정렬 문제
- `attached_assets/IMG_3785_1768890801948.png`: registered 페이지 헤더 문제
- `attached_assets/IMG_3786_1768890801948.png`: review 페이지 테이블 잘림 문제

---

## 3. 권장 브레이크포인트

### 3.1 2025 표준 브레이크포인트
| 디바이스 | 브레이크포인트 | 커버리지 |
|----------|---------------|----------|
| 모바일 소형 | 320px | 구형 iPhone |
| 모바일 | 480px | 최신 스마트폰 (~50% 모바일 트래픽) |
| 태블릿 세로 | 768px | iPad, Android 태블릿 (~18%) |
| 태블릿 가로/소형 데스크톱 | 1024px | 대형 태블릿, 소형 노트북 |
| 데스크톱 | 1280px+ | 표준 데스크톱 |

### 3.2 본 프로젝트 적용 브레이크포인트
```
모바일:     ~480px   (스마트폰)
태블릿:     ~768px   (아이패드 세로)
데스크톱:   1024px~  (노트북/PC)
```

### 3.3 적용 방식
**Desktop-First (max-width) 접근법**
- 기본 스타일 = 데스크톱 (기존 CSS 유지)
- 화면이 작아지면 레이아웃 조정
- 이유: 기존 style.css가 데스크톱 우선으로 작성되어 있어 max-width로 오버라이드하는 것이 가장 효율적
- 참고: 신규 프로젝트는 min-width(모바일 우선) 권장

---

## 4. 수정 대상 파일

| 파일 | 수정 내용 | 우선순위 |
|------|----------|----------|
| `static/style.css` | 미디어 쿼리 추가, 헤더/버튼/테이블 반응형 스타일 | 🔴 높음 |
| `templates/index.html` | 헤더 구조 개선 (nav → flex-wrap) | 🔴 높음 |
| `templates/registered.html` | 헤더 + 테이블 반응형 처리 | 🔴 높음 |
| `templates/review.html` | 헤더 + 테이블 반응형 처리 | 🔴 높음 |
| `templates/batch_collection.html` | 헤더 구조 확인 | 🟡 중간 |
| `templates/test_tool.html` | 버튼 그룹 반응형 | 🟢 낮음 |

---

## 5. CSS 변경 상세 계획

### 5.1 헤더 반응형 (제목 아래 버튼 배치)

**현재 코드:**
```css
header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-bottom: 20px;
    border-bottom: 2px solid var(--border-color);
    margin-bottom: 30px;
}
```

**개선안:**
```css
/* 기본 (모바일) */
header {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 15px;
    padding-bottom: 20px;
    border-bottom: 2px solid var(--border-color);
    margin-bottom: 30px;
}

header nav {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    width: 100%;
}

/* 태블릿 이상 */
@media (min-width: 768px) {
    header {
        flex-direction: row;
        justify-content: space-between;
        align-items: center;
    }
    header nav {
        width: auto;
    }
}
```

### 5.2 버튼 모바일 최적화

```css
/* 모바일 버튼 크기 조정 */
@media (max-width: 768px) {
    .btn {
        font-size: 0.85rem;
        padding: 10px 14px;
        min-height: 44px; /* 터치 타겟 최소 크기 */
    }
    .btn-sm {
        font-size: 0.8rem;
        padding: 8px 12px;
        min-height: 38px;
    }
}
```

### 5.3 테이블 반응형 (가로 스크롤)

```css
/* 테이블 컨테이너 */
.table-responsive {
    width: 100%;
    overflow-x: auto;
    -webkit-overflow-scrolling: touch;
}

@media (max-width: 768px) {
    table {
        min-width: 800px; /* 최소 너비 보장 */
    }
    th, td {
        padding: 10px 8px;
        font-size: 0.85rem;
        white-space: nowrap;
    }
    .thumb-img {
        width: 60px;
        height: 60px;
    }
}
```

### 5.4 통계 카드 반응형

```css
/* 기본 (모바일) - 세로 배치 */
.stats-box {
    display: flex;
    flex-direction: column;
    gap: 10px;
    margin-bottom: 30px;
}

.stat-card {
    padding: 15px;
}

.stat-number {
    font-size: 2rem;
}

/* 태블릿 이상 - 가로 배치 */
@media (min-width: 768px) {
    .stats-box {
        flex-direction: row;
        gap: 20px;
    }
    .stat-card {
        padding: 20px;
    }
    .stat-number {
        font-size: 2.5rem;
    }
}
```

### 5.5 메뉴 그리드 (이미 반응형)

현재 `menu-grid`는 이미 반응형 처리됨:
```css
.menu-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 20px;
}
```
→ 추가 수정 불필요

---

## 6. HTML 구조 변경

### 6.1 테이블 래퍼 추가

각 테이블을 `.table-responsive` div로 감싸기:

**변경 전:**
```html
<table>
    ...
</table>
```

**변경 후:**
```html
<div class="table-responsive">
    <table>
        ...
    </table>
</div>
```

### 6.2 적용 대상 페이지
- `templates/registered.html`
- `templates/review.html`
- `templates/batch_collection.html`

---

## 7. 작업 체크리스트

### 7.1 사전 확인
- [x] viewport 메타 태그 확인: `width=device-width, initial-scale=1.0`
- [x] Bootstrap 5 로드 확인
- [x] 현재 CSS 구조 분석
- [x] 문제 스크린샷 수집

### 7.2 구현 단계
- [ ] `static/style.css`에 미디어 쿼리 추가
- [ ] 헤더 반응형 스타일 적용
- [ ] 버튼 모바일 최적화
- [ ] 테이블 반응형 처리 (HTML + CSS)
- [ ] 통계 카드 반응형 적용
- [ ] 모바일 테스트 (Chrome DevTools)
- [ ] 실제 기기 테스트

---

## 8. 테스트 계획

### 8.1 테스트 환경
| 디바이스 | 해상도 | 테스트 방법 |
|----------|--------|-------------|
| iPhone SE | 375×667 | Chrome DevTools |
| iPhone 14 Pro | 393×852 | Chrome DevTools |
| iPad | 768×1024 | Chrome DevTools |
| Desktop | 1920×1080 | 브라우저 |

### 8.2 테스트 항목
- [ ] 헤더 버튼 정렬 확인
- [ ] 테이블 가로 스크롤 동작
- [ ] 터치 타겟 크기 (44×44px 이상)
- [ ] 통계 카드 레이아웃
- [ ] 메뉴 그리드 반응형

---

## 9. 참고 자료

### 9.1 2025 반응형 디자인 베스트 프랙티스
- **Mobile-First**: 전 세계 웹 트래픽의 58-70%가 모바일
- **Flexbox + flex-wrap**: 버튼 그룹에 가장 적합
- **min-width 미디어 쿼리**: 기본=모바일, 확장=데스크톱
- **터치 타겟**: 최소 44×44px로 터치 편의성 확보
- **clamp() 함수**: 유동적 폰트 크기

### 9.2 외부 참고 링크
- [Responsive Design Breakpoints 2025 - BrowserStack](https://www.browserstack.com/guide/responsive-design-breakpoints)
- [9 Responsive Design Best Practices for 2025](https://nextnative.dev/blog/responsive-design-best-practices)
- [CSS Flexbox Responsive - W3Schools](https://www.w3schools.com/css/css3_flexbox_responsive.asp)

---

## 10. 예상 작업 시간

| 단계 | 예상 시간 |
|------|----------|
| CSS 미디어 쿼리 추가 | 10분 |
| 헤더/버튼 반응형 | 10분 |
| 테이블 반응형 | 10분 |
| 테스트 및 미세 조정 | 10분 |
| **총계** | **약 40분** |

---

## 11. 개발센터 모달 반응형 (v1.9.5)

### 11.1 문제점
- 모바일에서 사이드바(240px)가 화면 50% 이상 차지
- 콘텐츠 영역이 좁아서 텍스트 잘림
- 모달 패딩이 모바일에 비해 큼

### 11.2 해결 방안: 햄버거 메뉴 토글

**레이아웃 변화:**
```
[데스크톱]                    [모바일]
┌──────┬─────────────┐       ┌─────────────────┐
│사이드│             │       │ ☰ 개발 센터     │
│ 바   │   콘텐츠    │  →    ├─────────────────┤
│      │             │       │                 │
│      │             │       │    콘텐츠       │
└──────┴─────────────┘       │    (전체 너비)  │
                              └─────────────────┘
```

### 11.3 CSS 변경사항

```css
/* 모바일 반응형 - 개발센터 모달 */
@media (max-width: 768px) {
    .notion-sidebar {
        position: fixed;
        left: -260px;
        top: 0;
        height: 100%;
        z-index: 1000;
        transition: left 0.3s ease;
    }
    
    .notion-sidebar.sidebar-open {
        left: 0;
    }
    
    .notion-content {
        padding: 15px 20px;
        width: 100%;
    }
    
    .sidebar-toggle {
        display: block;
        position: absolute;
        top: 10px;
        left: 10px;
    }
    
    .notion-title {
        font-size: 1.5em;
        margin-left: 40px;
    }
}
```

### 11.4 JavaScript 추가

```javascript
function toggleSidebar() {
    document.querySelector('.notion-sidebar').classList.toggle('sidebar-open');
}

// 메뉴 선택 시 사이드바 닫기 (모바일)
function showPage(pageId, navItem) {
    // 기존 로직...
    if (window.innerWidth <= 768) {
        document.querySelector('.notion-sidebar').classList.remove('sidebar-open');
    }
}
```

### 11.5 수정 대상 파일
- `templates/components/developer_log.html`: CSS 미디어 쿼리 + JS 토글 함수 추가

---

## 12. 변경 이력

| 날짜 | 버전 | 변경 내용 |
|------|------|----------|
| 2026-01-20 | v1.0 | 초안 작성 |
| 2026-01-20 | v1.1 | 개발센터 모달 반응형 섹션 추가 (v1.9.5) |
